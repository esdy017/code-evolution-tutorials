export default function PrivateRoute() {
  return (
    <>
      <h2>You cannot view this in browser</h2>
      <p>
        To include an underscore in an URL segment, prefix folder name with
        "%5F", which URL-encoded form of an underscore.
      </p>
    </>
  );
}
